---
apiVersion: v1
kind: Service
metadata:
   name: service-1
spec:
   type: LoadBalancer
   selector:
        app: web1 
   ports:
     - name: web1
       port: 80
       protocol: TCP
       targetPort: 8080